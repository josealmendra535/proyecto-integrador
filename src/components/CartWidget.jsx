import { IoIosCart } from "react-icons/io";

const CartWidget = () => {
    return (
        <div>
            <IoIosCart size={30} color="lime" />
            <p>2</p>
</div>
    );
};

export default CartWidget